# Only what is needed for: python reproduce_paper.py --toy
# The platform markers make this installable on Intel macOS as well as
# Apple Silicon, Linux, and Windows.
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
torch>=2.4,<3; sys_platform != "darwin" or platform_machine != "x86_64"
numpy>=1.26,<2
pandas>=2.2,<3
scipy>=1.14,<2
matplotlib>=3.9,<4
